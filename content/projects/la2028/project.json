{
  "slug": "la2028",
  "title": "LA28 Olympic Itinerary Planner",
  "tagline": "Turning LA28 ticket listings into a searchable planner with a shareable itinerary",
  "summary": "A pair of static HTML/CSS/JavaScript planners for the 2028 Los Angeles Olympics, built on ticket and schedule data that had to be extracted before it could be planned against. A Python script reads screenshots of the official LA28 Olympic Games Tickets listing page through an OpenAI vision model and writes strict-schema JSON; a Node script does the same job from a pasted text dump. The browser apps then let you search, filter, sort, browse a month calendar, build an itinerary, and see an estimated total from listed starting prices. Everything runs client-side: selections live in localStorage and export as JSON you can hand to someone else.",
  "published": true,
  "featured": false,
  "date": "2026-04",
  "status": null,
  "tech": [
    "HTML",
    "CSS",
    "Vanilla JavaScript",
    "localStorage",
    "Fetch API",
    "Python 3",
    "OpenAI Python SDK",
    "OpenAI vision model (gpt-4o)",
    "JSON Schema",
    "python-dotenv",
    "Node.js",
    "Google Fonts (DM Sans)"
  ],
  "links": [
    {
      "label": "Live demo",
      "href": "https://michael-lizzio.github.io/branch/la2028/index.html"
    }
  ],
  "hero": {
    "src": "media/la2028_planner.webp",
    "kind": "image",
    "poster": null,
    "alt": "LA28 itinerary planner showing 429 loaded events, event filters, session cards and an itinerary sidebar",
    "caption": "The main planner with the full 429-session extraction loaded."
  },
  "sections": [
    {
      "heading": "What it is",
      "body": [
        "A static HTML / CSS / JavaScript app. It reads ticket rows from tickets/extracted_tickets.json (produced by script_code/extract_tickets.py) and lets you search, filter, sort, browse a month calendar, build an itinerary, and see an estimated total from listed starting prices.",
        "The header sums up the job in one line: browse sessions, build your schedule, estimate ticket spend. A second build in branch/random splits the same idea into two pages — a family trip calendar driven by the published competition schedule, and a priced-listings browser driven by the ticket shop data.",
        "Nothing is served from a backend. Both builds fetch a JSON file, keep your picks in localStorage, and hand you a JSON download when you want to share the plan."
      ],
      "media": []
    },
    {
      "heading": "Getting the data out of a ticket site",
      "body": [
        "The hard part was never the calendar — it was that 429 priced sessions only existed as pages of a ticket shop. extract_tickets.py solves it with vision: \"Extract LA28 Olympic ticket rows from screenshots using OpenAI vision + structured JSON. Uses the Responses API with multimodal input (text + image) and strict json_schema output.\"",
        "The prompt is deliberately narrow: \"You extract structured data from screenshots of the official LA28 Olympic Games Tickets listing page. Each visible card/row usually has: a code + sport title line, a description line, a line with date | time | venue | zone, and a starting price. Copy text faithfully. If a field is missing or unreadable, use empty string for strings and null for price_amount when unknown. Include every ticket row fully visible in the image; do not invent rows beyond what is shown.\"",
        "A strict JSON Schema (schema.py) pins every row to event code, title, description, date, local time, venue, zone, display price, numeric price, and currency. The runner retries with exponential backoff, stamps each row with its source image, de-duplicates across pages, and rewrites a .partial file after every image so a long run can be watched or salvaged. The committed output records the run: 22 screenshots in, 429 ticket rows out, model gpt-4o — and the de-duplication found nothing to drop that time (429 raw rows, 429 unique).",
        "branch/random takes a second route to the same destination. build-tickets-json.mjs is a one-off Node parser — \"parse data/raw_data.txt → data/la28-ticket-listings.json\" — that regex-splits a pasted text dump of the shop into 337 unique events with ISO dates, venue/zone split on the pipe, a parsed starting price in USD, and the original tickets.la28.org link."
      ],
      "media": []
    },
    {
      "heading": "Two ways to plan the trip",
      "body": [
        "The main planner is a list/calendar toggle over the extracted rows. Filters cover sport, zone, day and a maximum price; six sort orders cover date, price and title; search matches title, code, venue, zone, description and time. Sessions get an emoji from the first three letters of their event code, falling back to a keyword match on the title. Days with sessions are counted on a month grid, and clicking a day filters the list to it. The itinerary panel groups picks by date, saves them under the localStorage key la28-itinerary-v1, and totals the listed prices with an honest caveat: \"Sum of listed starting prices only; not an official quote.\" There is a light/dark toggle, a JSON export of just the selected events, and a Load JSON file fallback for when fetch is blocked.",
        "The family trip calendar in branch/random starts from the trip instead of the catalogue: \"Set your arrival and departure — everything outside that window disappears.\" The two date fields open on 14–30 July 2028 and are clamped to the Games window (10 July to 15 August 2028). It renders only the sports the family actually discussed — track and field, lacrosse, swimming, equestrian, beach volleyball, artistic swimming, water polo, climbing, sailing, rhythmic, triathlon — as a colour-coded Sun-first grid of the trip's days, with a Colors legend tucked into a details panel. The equestrian filter is opinionated by design: eventing (EQU01–EQU05) and jumping (EQU13–EQU16) are shown, and \"Pure dressage sessions (EQU06–EQU12) are hidden on purpose.\" Tap a session to shortlist it, flip the radio from \"All sessions in your trip dates\" to \"Only what I tapped (my list)\" to see just the shortlist, use the quick-add buttons for the known must-sees, then download the picks as JSON with a name and notes attached.",
        "The third page browses every priced listing: \"Search and filter, add sessions to My itinerary, and see codes, venues, and starting prices. Selections save in this browser.\" It groups by date by default, sorts lowest or highest price on request, and totals the shortlist as a sum of \"from\" prices — again labelled as \"Not a checkout total — starting prices only.\""
      ],
      "media": []
    },
    {
      "heading": "Laptop-first, phone-friendly",
      "body": [
        "The stylesheet states its own brief on line one: \"LA28 Itinerary Planner — laptop-first, responsive\". On a wide screen the planner is a two-column grid — the event list on the left, a 380px itinerary rail held at the right. Below 1100px the grid collapses to a single column, the rail turns into a bottom sheet that slides up from a floating Itinerary button carrying a live count, and the filter row folds behind a Filters toggle.",
        "It ships dark-first — a deep navy ground with an amber accent — with a light theme (white cards, burnt-orange accent) supplied by an html[data-theme=\"light\"] override. An inline script in the head reads the la28-theme key out of localStorage before the page paints, falling back to the system prefers-color-scheme, so the toggle never flashes the wrong theme. A status banner under the header reports how many events loaded, and switches to a warning when the payload's own meta says not every screenshot was processed."
      ],
      "media": []
    }
  ],
  "notes": "REPAIR PASS (2026-09-13): A fresh screenshot of the running legacy planner was captured, compressed, and installed as the hero; the GitHub Pages demo was verified and linked. The date 2026-04 is inferred from the first path-specific git commit on 2026-04-09. The historical extraction notes below are retained for provenance, but their no-screenshot and no-hero statements are superseded.\n\nONE PROJECT, TWO BUILDS — not two products. Both folders are LA28 session planning by the same author, committed together in the single repo commit 081ec8c 'Added all tickets and prices'. branch/la2028 is the screenshot-OCR planner (one page, list + calendar + itinerary). branch/random is a second pass split across two pages: index.html (family trip calendar off the published competition schedule) and tickets.html (priced listings browser off the pasted ticket dump). They share no code and use three different localStorage keys (la28-itinerary-v1, la28-family-planner-v2, la28-ticket-itinerary-v1), so the itineraries do not sync between them. The folder name 'random' is not descriptive of its contents. Slug uses the legacy folder name 'la2028' (note: the folder is la2028, the brand in the copy is LA28).\n\nHERO / SCREENSHOTS: no screenshot of either app exists anywhere in the media manifest, so hero is null — A REAL APP SCREENSHOT IS NEEDED. The only media in the manifest is branch/la2028/images/*.png: 22 tall PNGs that are captures of the official LA28 Olympic Games Tickets shop (pages 1–22 of the listing, one per capture), used as OCR input, not pictures of this project. Only one of the 22 is referenced here — the rest are near-identical pages of the same third-party UI and add nothing a reader needs. I used one in section 2 because it genuinely shows the pipeline's input, but it is third-party UI carrying LA28 branding and live prices — consider dropping it, or replacing it with a screenshot of the planner, before publishing.\n\nMEDIA REFERENCES: none to fix. No <img> tag and no CSS url() appears in any of the three HTML files or three stylesheets; sport icons are emoji from data/sport-icons.js, not image files. Every listing row in la28-ticket-listings.json carries an `image` URL on the tickets.la28.org CDN, but tickets-app.js never renders it — dead data, not a broken reference.\n\nUNUSED DATA: branch/random/data also holds LA28CompetitionScheduleByDayDetailed.json and LA28CompetitionScheduleByEventDetailed.json. Nothing in either build references them — planner-app.js fetches only LA28CompetitionScheduleByVenueSportSessionDetailed.json — so they are committed reference exports, not wiring.\n\nRUNTIME: all three pages fetch() JSON and therefore must be served over HTTP; opened as file:// each shows its own message, and the wording differs per page — tickets.html: 'Could not load la28-ticket-listings.json. Serve this folder over HTTP (e.g. GitHub Pages), not file://.'; branch/random/index.html: 'Could not load the schedule file. Open this site over HTTP (GitHub Pages or a local server), not as a raw file.'; branch/la2028/index.html degrades instead of dying — 'Could not load JSON (...). Use “Load JSON file” or run a local server from this folder.' — and you can still pick a .json off disk. README documents `python -m http.server 8080` from branch/la2028. branch/la2028 supports ?partial=1 to load ./tickets/extracted_tickets.json.partial instead of the final file; that .partial file is NOT in the repo (the script deletes it on success), so ?partial=1 will 404 — expected, not a bug.\n\nPRIVACY / SCRUB BEFORE PUBLISHING: branch/random copy names real people — 'Name (optional, for the file you send Michael)', 'Export JSON to share with Michael', 'Pink border = [a family member] overlap (ATH03 / LAC05)', hard-coded quick picks labelled '[a family member] track day' / '[a family member] lacrosse day', a code comment 'that matches \"jumping + eventing\" for [a family member]', and the name field's own placeholder 'e.g. [a family member]'. Also, data/raw_data.txt is a raw pasted chat message and opens with the author's own typo-ridden prompt ('wait can yu make a json of all avaiable tickets'); do not surface that file verbatim.\n\nDATA CAVEATS: all 429 extracted rows and all 337 parsed listings carry 2028 dates, and every one of them has a numeric starting price — 'starting at' figures captured in April 2026, not checkout prices. No time field literally reads 'TBC': 36 of the 429 rows have the placeholder '00:00 Local Time (24-hour)' (10 of them written '0:00'), and it is the description line that says 'Start and end times TBC' on 37 rows. The vision pass is visibly lossy, which matters if this dataset is ever shown as fact: comparing the 429 OCR rows against the 337 rows the Node parser got from the pasted text, 58 of the 319 shared session codes disagree on date and 50 disagree on price. Reading the source screenshots settles it in the text parser's favour — page 1 shows FBL01 on 07.10.2028, HBL01/CKT01/RU701/BKB01 on 07.12.2028 and RU701 'Starting at $111.63', while extracted_tickets.json records 07.20.2028, 07.22.2028 and $11.63. The misreads cluster as a day-digit slip (07.1x read as 07.2x) plus the odd dropped digit in a price. Both apps label their totals as estimates, not quotes. The competition-schedule JSON is itself derived ('machine-readable venue-and-sport grouping derived from the detailed event schedule') from two LA28 PDFs.\n\nCODE QUIRKS A PORTER SHOULD KNOW: .env.example asks for OPENAI_API_KEY (none committed) and suggests OPENAI_MODEL=gpt-5.4-mini, but the script default is gpt-4o and the committed extraction was actually run on gpt-4o. planner-app.js matches sport names by exact string including a literal newline — \"Sailing (Dinghy, Skiff &\\nMultihull)\" — which will silently drop sessions if the data is ever reformatted. app.js guesses ambiguous DD.MM vs MM.DD dates and badges unparseable ones 'Date?'. IDs in branch/la2028 are a hand-rolled 32-bit hash of code+date+time+venue+title, so saved itineraries are dropped when the dataset changes. Committed __pycache__/*.pyc under script_code/ should not be carried over. The two branch/random pages link to each other with relative hrefs (index.html <-> tickets.html) that only resolve inside that folder."
}
